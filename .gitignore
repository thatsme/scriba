scriba-*.tar

# Build / deps
/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez

# Tooling
/.elixir_ls/
/.lexical/
.dialyzer_plt
.dialyxir_*

# Editor / OS
.DS_Store
Thumbs.db

# Local secrets (real credentials for SCRIBA_TEST_DB_*; see .env.local.example
# for the template). Never commit.
.env.local
.env.*.local
